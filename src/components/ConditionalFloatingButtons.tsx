import { FloatingCartButton } from "./FloatingCartButton";

export const ConditionalFloatingButtons = () => {
  // Always show cart button for easy access, especially on mobile
  return <FloatingCartButton />;
};
